import { expect, it } from "@jest/globals";
import * as notificationService from "../src/services/demo";

describe("demo service", () => {
    it("runs a test", () => {
        //arrange
        //act
        //assert
        expect(1).toBe(2);
    });
});
